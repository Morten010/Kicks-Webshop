import { FC } from 'react'

interface Custom500pageProps {
  
}

const Custom500page: FC<Custom500pageProps> = ({}) => {
  return <div>
    <h1>
        Something went wrong
    </h1>
  </div>
}

export default 500